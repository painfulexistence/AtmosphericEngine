{
	"version": "2.0.0",
	"tasks": [
        {
            "type": "shell",
            "label": "Building with shell script",
            "command": "./build.sh",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}